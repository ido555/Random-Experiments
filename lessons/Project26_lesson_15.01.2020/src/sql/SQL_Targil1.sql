-- display ALL data of products

-- display the id, product_code, product_name list_price, quantity_per_unit of all products with list_price bigger than 30

-- display all data for products with supplier_id of 10 or 6

-- display all data for products with units in boxes   

-- display 
-- 1. select * from products;
-- 2. select id, product_code, product_name, list_price, quantity_per_unit from products where list_price > 30;
-- 3. select * from products where supplier_ids in (10,6)
-- 4. select * from products where quantity_per_unit like '%box%'