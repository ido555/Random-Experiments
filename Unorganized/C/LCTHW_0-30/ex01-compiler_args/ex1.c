#include <stdio.h>

int main(int argc, char *argv[])
{
    int distance = 100;

    printf("you are %d kilometers away", distance);

    return 0;

    // 
    /* GCC Arguments:
    https://gcc.gnu.org/onlinedocs/gcc
    -Wall 
    This enables all the warnings about constructions that some users consider questionable
    and that are easy to avoid

    -g
    Produce debugging information
    
    -o
    Write the build output to an output file. (like a terminal/cmd/console)
    */
}